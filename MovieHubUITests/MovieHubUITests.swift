//
//  MovieHubUITests.swift
//  MovieHubUITests
//
//  Created by Abhijeet Banarase on 07/12/2023.
//

import XCTest
/*
final class MovieHubUITests: XCTestCase {

    var app: XCUIApplication!

    override func setUpWithError() throws {
        continueAfterFailure = false
        app = XCUIApplication()
        app.launch()
    }

    override func tearDownWithError() throws {
        app = nil
    }

    func testTabBarItemsExist() {
        let tabBar = app.tabBars["Tab Bar"]
        XCTAssertTrue(tabBar.exists)
        
        let movieTab = tabBar.buttons["Movie"]
        XCTAssertTrue(movieTab.exists)
        
        let searchTab = tabBar.buttons["Search"]
        XCTAssertTrue(searchTab.exists)
    }

    func testSwitchTabViews() {
        let movieTab = app.tabBars.buttons["Movie"]
        XCTAssertTrue(movieTab.waitForExistence(timeout: 5))
        movieTab.tap()
        
        let movieNavigationBar = app.navigationBars["Movies"]
        XCTAssertTrue(movieNavigationBar.exists)
        
        let searchTab = app.tabBars.buttons["Search"]
        XCTAssertTrue(searchTab.waitForExistence(timeout: 5))
        searchTab.tap()
        
        let searchNavigationBar = app.navigationBars["Search"]
        XCTAssertTrue(searchNavigationBar.exists)
    }
}
*/
